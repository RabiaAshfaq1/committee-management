/**
 * Default production build (e.g. same-origin reverse proxy).
 * For Vercel, use `npm run build:vercel` (sets apiUrl from NG_APP_API_URL).
 */
export const environment = {
  production: true,
  apiUrl: '/api',
};
